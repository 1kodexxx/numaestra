-- Демо как часть финального заказа: сохраняем ПОЛНЫЕ клипы демо-задачи, чтобы
-- после оплаты переиспользовать их как версии 1–2 и догенерировать только 3–4.
-- Это даёт клиенту ровно ту песню, что он слушал в демо, и экономит одну Suno-
-- задачу на конверсию (демо-задача больше не «выбрасывается»).
--
-- Изоляция от платёжного пути сохранена: колонку пишет ТОЛЬКО демо-поток
-- (UpdateDemo). Полные клипы лежат в JSONB (а не в таблице tracks), поэтому до
-- завершения заказа они никогда не попадают в выдачу API.
ALTER TABLE orders
    ADD COLUMN IF NOT EXISTS demo_clips JSONB;
